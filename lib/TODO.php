get_users_by_auth0id
get_currentauth0userinfo
get_currentauth0user
get_current_user_profiles

one profile per user (go back with users link, update the rule to merge metadata)
change how it gets the profile, now comes from wp_user metadata

move errors to custom post